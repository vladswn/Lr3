@model Project.Models.Course

@{ 
    Layout = "~/Views/Shared/_LayoutDetailPage.cshtml";
}

<div class="container" style="margin-top:51px;">
    @*<div class="row">
        <div class="col-md-12" style="border:1px solid black;">
            <ol class="breadcrumb" style="height:100%;">
                <li>@Html.ActionLink("Home", "Index", "Home")</li>
                <li>@Html.ActionLink("Courses", "Courses", "Home")</li>
            </ol>
        </div>
    </div>*@
    <div class="row header_detail">
        <div class="col-md-3">
            <img  src="https://www.edx.org/sites/default/files/styles/course_video_banner/public/course/image/featured-card/course_image_csmm_101x_318x210.jpg?itok=CvQLn7f_" class="img-responsive" />
        </div>
        <div class="col-md-6">
            <h2>@Model.Title</h2>
            <p style="font-size:14pt;">
                @Model.DescriptionCourse
            </p>
        </div>
        <div class="col-md-3 header_date_block">
            <h4>
                <b>Начало - @Model.SrartDate.ToLongDateString()</b>
            </h4>

            @foreach (var user in ViewBag.User)
            {
            @*@User.Identity.Name
            @user.Id*@
            if (Model.Users.Where(s => s.UserName == User.Identity.Name).FirstOrDefault() != null)
            {
                if (Model.Users.Where(s => s.UserName == User.Identity.Name).FirstOrDefault().Courses.Contains(Model)/* && Model.Users.Where(s => s.UserName == User.Identity.Name).FirstOrDefault() != null*/)
                {
                    @Html.ActionLink("Перейти к курсу", "Task", "Home", new { task = Model.CourseId }, new { @class = "btn btn-primary btn-block btn-lg" })
                    @Html.ActionLink("Отписаться от курса", "TaskDelete", "Home", new { task = Model.CourseId }, new { @class = "btn btn-danger btn-block btn-lg" })
               }
            }
                        else
                        {
                            @Html.ActionLink("Подписаться на курс", "AddCourse", "Home", new { id = Model.CourseId }, new { @class = "btn btn-primary btn-block btn-lg modal_item" })
            

                }
                        break;
                    }

            @*@Html.ActionLink("Подписаться на курс", "AddCourse", "Home", new { id = Model.CourseId }, new { @class = "btn btn-primary btn-block btn-lg modal_item" })*@
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-md-8" style="border:1px solid #d9d9d9;
    background: #e6e6e6;">
            qweqwe
            qweqw
            <br />
            qweqw
            <br />
            qweqw
            <br />
            qweqw
            <br />
            qweqw
            <br />
            qweqw
            <br />
        </div>

        <div class="col-md-3 col-md-offset-1" @*style="padding-bottom:10px;"*@>
            <div class="row" style="margin-bottom:10px;
    border: 1px solid #d9d9d9;
    background: #e6e6e6;">
                <div class="col-md-12">
                    qweqw
                    <br />
                    qweqw
                    <br />
                </div>
            </div>

            <div class="row" style="margin-bottom:10px;
    border: 1px solid #d9d9d9;
    background: #e6e6e6;">
                <div class="col-md-12">
                    qweqw
                    <br />
                    qweqw
                    <br />
                </div>
            </div>

            <div class="row" style="margin-bottom:10px;
    border: 1px solid #d9d9d9;
    background: #e6e6e6;">
                <div class="col-md-12">
                    qweqw
                    <br />
                    qweqw
                    <br />
                </div>
            </div>
        </div>
    </div>
</div>

<div id="modDialog" class="modal fade">
    <div id="dialogContent" class="modal-dialog"></div>
</div>

    <script type="text/javascript">
        $(function () {
            $.ajaxSetup({ cache: false });
            $(".modal_item").click(function (e) {

                e.preventDefault();
                $.get(this.href, function (data) {
                    $('#dialogContent').html(data);
                    $('#modDialog').modal('show');
                });
            });
        })
    </script>


